//$service999.test.withsyncedinvoke

//#receive
    
    var ret :InvokeResult = null

    ret = syncedInvoke("kafkaproducer.write",3000,
        "topic" -> "test",
        //"key" -> "abc",
        "value" -> ( "hello world : " + System.currentTimeMillis ) )

    println(ret.toString)

    reply(0)
  

